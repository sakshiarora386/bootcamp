summary: Update User
tasks:
  - id: gs_bootcamp_user_update
    fn: datasource.gs_bootcamp.User.update
    args:
      where:
        user_id: <% parseInt(inputs.params.user_id) %>
      data: <% inputs.body %>
